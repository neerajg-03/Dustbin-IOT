        if assigned_driver_number:
            try:
                client.messages.create(
                    body=f"🚛 Alert: {assigned_van}, collect from {bin['Bin ID']} (Fill: {bin['Fill Level (%)']}%).",
                    from_=TWILIO_PHONE_NUMBER,
                    to=assigned_driver_number
                )
            except Exception as e:
                st.error(f"Twilio Error: {e}")